# Security Policy

## 项目安全边界

本项目是运行在浏览器和 Tampermonkey 中的用户脚本。API Key 只应保存在用户自己的 Tampermonkey 本地存储中，不应写入源码、Issue、Pull Request、日志截图或配置示例。

项目导出的普通备份不应包含 API Key；提交问题前仍应人工检查导出文件和截图，避免包含账号信息、Cookie、Token、浏览历史或其他个人数据。

## 建议报告的问题

- 未经用户确认发送额外页面数据；
- API Key、Token 或隐私数据意外暴露；
- 可导致任意脚本执行的注入问题；
- 导入、导出或更新流程造成的数据泄露；
- GitHub Actions 或发布链路中的供应链风险。

## 报告方式

请优先使用 GitHub 的私密安全报告功能（如果仓库已启用）。如果没有私密渠道，请在公开 Issue 中只描述问题类型和影响，不要附带真实密钥、Cookie、个人数据或可直接利用的敏感细节。

如果凭据已经泄露，应先在对应服务商处撤销或轮换，再处理仓库和 Git 历史中的残留内容。
